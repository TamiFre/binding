<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="binding.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:Models="clr-namespace:binding.Models"
    x:DataType="Models:Student">
    <StackLayout HorizontalOptions="Center">
        <!--<Label
            x:Name="label"
            BindingContext="{x:Reference OpacitySlider}"
            FontSize="40"
            HorizontalOptions="Center"
            Opacity="{Binding Value}"
            Text="שלום יא2" />

        <Button
            x:Name="btn1"
            BindingContext="{x:Reference HeightSlider}"
            HeightRequest="{Binding Value}"
            Text="lala" />


        <Slider
            x:Name="HeightSlider"
            BindingContext="{x:Reference btn1}"
            Maximum="60"
            Minimum="1"
            Value="{Binding Source={x:Reference btn1}, Path=HeightRequest}" />


        <Slider
            x:Name="OpacitySlider"
            Maximum="1"
            Minimum="0" />-->
        <Label Text="{Binding Name}" />
        <Label Text="{Binding Age}" />

        <Button Clicked="ChangeName" Text="Change name to Ziv" />



    </StackLayout>


</ContentPage>
